name: Run

on: 
  workflow_dispatch:

jobs:
  run:
    name: Run
    runs-on: ubuntu-latest
    steps:
      - name: Run
        run: |
          docker run atdr.meo.ws/archiveteam/telegram-grab --concurrent 20 Hans5958
        timeout-minutes: 60
